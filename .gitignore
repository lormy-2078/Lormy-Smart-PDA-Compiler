.env
data/pda_platform.db
storage/documents/
storage/outputs/
__pycache__/
*.pyc
.tariff_render/
.tr2/
data/*.bak.json

# Deliberately out of the published repository
# The Netlify build copy — a duplicate of the app and the tariff data.
netlify_deploy/
# Claude Code's per-machine settings: it embeds absolute local paths.
.claude/settings.local.json
