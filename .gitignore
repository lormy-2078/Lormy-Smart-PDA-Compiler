.env
data/pda_platform.db
storage/documents/
storage/outputs/
__pycache__/
*.pyc
.tariff_render/
.tr2/
data/*.bak.json

# The Netlify build copy — deleted 2026-09-04; if it comes back it is a build
# artefact, not source.
netlify_deploy/
# Claude Code's per-machine settings: it embeds absolute local paths.
.claude/settings.local.json
